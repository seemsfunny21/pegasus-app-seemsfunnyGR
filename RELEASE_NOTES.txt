PEGASUS 232 - Mobile stable sync text size

- Restored the mobile sync text to a larger, readable fixed size.
- Locked sync indicator width/height/line-height in CSS, adaptiveTypography, and runtime JS.
- Prevented non-heartbeat background sync events from visually switching the countdown into ΣΥΓΧΡΟΝΙΣΜΟΣ.
- Bumped manifest to v18.33 and permanent local cache to v232.

Install note:
Upload all files, then reload mobile once so sw.js?v=3.33.232 takes over.
