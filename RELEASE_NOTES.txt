PEGASUS 243 - Biometric HTTPS Safe Mode
- mobile/biometric-unlock-mobile.js updated to v1.3.243.
- Biometric unlock now stays dormant on non-HTTPS / non-secure origins instead of showing disruptive errors.
- Fingerprint/WebAuthn still requires HTTPS or localhost because Android/Chrome enforces this; Pegasus cannot bypass it safely.
- Service worker registration now runs only on secure context to avoid HTTPS-related registration errors on plain HTTP/file origins.
- Service worker cache bumped to 243.

PEGASUS 240 - Mobile Biometric Unlock Module

- Added new separate file: mobile/biometric-unlock-mobile.js.
- Mobile PIN vault can now show a fingerprint/passkey unlock button when the device is already approved.
- After a normal PIN + MASTER KEY unlock, Pegasus offers activation of Android/Chrome biometric unlock.
- Biometric unlock uses WebAuthn/platform authenticator; Pegasus never stores the fingerprint itself.
- Added a small mobile vault bridge so external modules can finish the existing PIN unlock flow safely.
- Social/Secure Vault PIN screen can also show a biometric button after biometric unlock has been enabled.
- Permanent local cache bumped to v240 and service worker registration bumped to 3.36.240.

PEGASUS 239 - Desktop Boot Supplement Sync Guard

Bugfix:
- Fixed protein/creatine refill still syncing according to the desktop when the PC opens after a mobile refill.
- Desktop boot writes for `pegasus_supp_inventory` are now blocked from entering the cloud pending queue before the first pull.
- Existing stale desktop supplement pending entries are pruned before pull/push when the cloud already has supplement inventory.
- Before any desktop push, cloudSync performs a final per-field merge with the current cloud supplement inventory so the newest mobile protein/creatine value wins.
- Permanent local cache bumped to v239 and service worker registration bumped to 3.36.239.

Expected behavior:
- Refill protein on mobile to 2500g.
- Open desktop after sync.
- Desktop first pulls/merges, then shows 2500g and cannot push its stale old stock back to cloud.

PEGASUS 238 - Supplement Refill Sync Timestamp Fix

Bugfix:
- Fixed mobile supplement refill being overwritten by the desktop's older protein/creatine value.
- `pegasus_supp_inventory` now carries per-field timestamps: `protUpdatedAt` and `creaUpdatedAt`.
- Cloud sync now merges protein and creatine stock by newest field update instead of blindly replacing the object.
- Mobile refill, mobile consume/restore, PC consume/restore/restock, and PC inventory guard preserve legacy keys while updating sync metadata.
- Permanent local cache bumped to v238 and service worker registration bumped to 3.36.238.

Expected behavior:
- Refill protein on mobile to 2500g.
- Open desktop after sync. Desktop should keep 2500g instead of reverting to its old local value.
- Future whey/creatine consumption and restore operations should sync in the correct direction by latest edit.

PEGASUS 237 - Morning Report Full Backup

Problem fixed:
- The user wants the daily morning report to also send a complete backup of PEGASUS data so future data loss can be recovered from the email.

Changes:
- Added PEGASUS 237 Daily Backup Collector in backup.js.
- Morning report now builds a fresh backup at send time, not the previous evening.
- Backup is injected into existing EmailJS template data through nutrition_advice so it is sent even if the EmailJS template has not been edited to add a new backup field.
- Additional params are also filled: backup_summary, backup_json, pegasus_backup_json, backup_schema, backup_key_count, backup_size_bytes, backup_truncated.
- Backup includes PEGASUS user data keys, food logs, cardio logs, weight keys, Kouki keys, parking, biometrics, lifting, car/service data, finance, notes, supplies, movies, YouTube, missions, social/contacts where present.
- Backup deliberately skips PIN/master/API/vault secrets, runtime logs, sync traces, old backups/snapshots and permanent-cache markers to avoid unsafe leaks and recursive oversized emails.
- Manual export filename is now PEGASUS_FULL_BACKUP_YYYY-MM-DD.json and uses the same complete user-data collector.
- Permanent local cache bumped to v237.

Install note:
Upload all files. First open after this package will build/check v237 local cache once. The next morning report should log: "PEGASUS REPORT: Daily backup attached" before sending.

Package:
- Code-only.
- No images folder.
- No videos folder.

PEGASUS 241 - Mobile Biometric Enrollment Fix
- Improved mobile/biometric-unlock-mobile.js v1.1.241.
- Biometric enrollment prompt now appears after manual unlock and approved-device auto unlock.
- Added persistent Settings card: ΒΙΟΜΕΤΡΙΚΟ ΞΕΚΛΕΙΔΩΜΑ, with enable/test/reset controls.
- PIN modal now always shows biometric unlock when enrolled.
- Added PegasusBiometricUnlock.status(), showPrompt(), reset() helpers for console debugging.
- Service worker cache bumped to 241.


PEGASUS 242 - Mobile Load Fix for Biometric Module
- Fixed mobile load/freeze caused by biometric settings card re-render loop.
- mobile/biometric-unlock-mobile.js updated to v1.2.242 with render guard and throttled MutationObserver.
- Keeps separate biometric module and settings option, but prevents continuous DOM rewrites.
- Service worker cache bumped to 242.
